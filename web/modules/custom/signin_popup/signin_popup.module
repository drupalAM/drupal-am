<?php

/**
 * Implements hook_theme().
 */
function signin_popup_theme(){
  $user_id = \Drupal::currentUser()->id();

  return [
    'join_now_block' => [
      'variables' => [
        'title' => 'Default title',
        'is_anonymous' => NULL,
      ],
    ],
  ];
}

